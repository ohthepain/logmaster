app_identifier('live.logmaster.app')

# Developer Portal (code signing)
team_id('RPGSNMH65P')

# App Store Connect — "Paul Wilkinson" (selected during TestFlight upload)
itc_team_id(ENV.fetch('FASTLANE_ITC_TEAM_ID', '13820'))

apple_id(ENV.fetch('APPLE_ID', 'cremoni@gmail.com'))
